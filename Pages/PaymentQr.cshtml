@page
@model PaymentQrModel
@{
    ViewData["Title"] = "Thanh toán bằng ZaloPay";
}

<div class="container mt-5">
    <div class="card shadow-sm border-0">
        <div class="card-body text-center">
            <h2 class="card-title mb-3">Thanh toán bằng VnPay</h2>
            <p class="text-muted mb-4">Nhấn bên dưới để thanh toán qua VnPay.</p>
            @if (!string.IsNullOrEmpty(Model.QrUrl))
            {
                <img src="@Model.QrUrl" alt="VnPay QR" class="img-fluid" style="max-width:300px;" />
            }
            <div class="mt-4">
                <a class="btn btn-primary btn-lg" href="@Model.PaymentUrl" target="_blank">Mở VnPay & Thanh toán</a>
            </div>
            <p class="text-muted mt-3">Hoặc nếu điện thoại không quét được, bấm nút trên để chuyển sang cổng VnPay.</p>
        </div>
    </div>
</div>